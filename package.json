{
  "name": "flexirule-docs",
  "version": "1.0.0",
  "description": "Documentation for FlexiRule",
  "scripts": {
    "start": "hugo server --disableFastRender",
    "build": "hugo --gc --minify",
    "lint": "eslint .",
    "clean": "rm -rf public resources"
  },
  "devDependencies": {
    "@hyas/doks-core": "^1.2.0",
    "hugo-bin": "^0.116.1"
  }
}
